/**
 * Academy LMS — Design System
 * Central export. Import from here, never from individual token files.
 *
 * Usage:
 *   import { spacing, shadows, typography } from '@/design-system'
 */

export * from './tokens'
